Проект "Project OOP"

---

## Описание:
На данном этапе работы осуществляется подготовление функций для того, чтобы появилось ядро для интернет-магазина. 
В дальнейшем для этого ядра возможно будет реализовать любой интерфейс — от сайта до телеграм-бота.
---

## Установка:
1. Клонируйте репозиторий:
```
git@github.com:kaverina0208-builder/Project_OOP.git
```
2. Установите зависимости:
```
pip install -r requirements.txt

```

___
## Использование:
В файле ***category.py*** описан класс ***Category*** с атрибутами ***name, description, products***
В файле ***products.py*** описан класс ***Product*** с атрибутами ***name, description, price, quantity***
Созданы два новых наследуемых класса от класса Product:
В файле ***smartphone.py*** описан класс ***Smartphone*** с атрибутами efficiency, model, memory, color.
В файле ***lawngrass.py*** описан класс ***LawnGrass*** с атрибутами country, germination_period, color.
Также реализована функциональность сложения таким образом, чтобы можно было складывать товары только 
из одинаковых классов продуктов. Доработан метод, который добавляет продукт в категорию таким образом, 
чтобы не было возможности добавить вместо продукта или его наследников любой другой объект.
Добавлен новый абсрактный класс ***BaseProduct*** и класс миксин ***PrintMixin*** 
___

## Тестирование:
В файлах ***test_category.py*** и ***test_product*** написаны тесты для новых классов.
В файлах ***test_smartphone.py*** и ***test_lawngrass.py*** написаны тесты для новых классов.
Для новых классов реализованы тесты в модуле ***test_print_mixin***